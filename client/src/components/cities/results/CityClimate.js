import React, { Component } from "react";

class CityClimate extends Component {
  render() {
    // city weather avgs and type in title
    // multi pi chart for # sunny days, chance of sunshine + day lengths
    return <div />;
  }
}

export default CityClimate;
